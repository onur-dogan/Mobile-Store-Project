import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: "red",
        alignItems: "center"
    },
    text : {
        fontSize: 20,
        fontWeight: "bold",
        color:"white"
    }
  });

  export default styles