import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        paddingVertical: 20,
        paddingHorizontal: 20,
        elevation: 4,
        borderWidth: 1,
        borderColor: "red",
        backgroundColor: "#E3EE07",
        marginBottom: 2
    },
    image : {
    alignItems:"center",
    width: 50,
    height: 50,
    }
});

export default styles