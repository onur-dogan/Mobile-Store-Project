/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {Content} from './src/navigations/App'

AppRegistry.registerComponent(appName, () => Content);
